<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.mynote"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".App"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".module.gesture.activity.GestureVerifyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".module.gesture.activity.GestureEditActivity"/>
        <activity android:name=".module.gesture.activity.GestureUpdateActivity"/>
        <activity android:name=".module.gesture.activity.SuperPassWordActivity"/>
        <activity android:name=".module.home.activity.MainActivity" android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".module.home.activity.AddDataActivity"/>
        <activity android:name=".module.home.activity.ImportDataActivity"/>
    </application>

</manifest>